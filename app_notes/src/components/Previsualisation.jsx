import React from 'react';

const Previsualisation = () => {
    return (
        <div>
            
        </div>
    );
};

export default Previsualisation;

// remette les variables de showdown

// récupère la base de données ( data-note ) note.id vérifier si le param id correspond 

// stocke le contenu dde la note dans une variables


// afficher la variable avec le showdown + mettre le bouton retour et modifier.

